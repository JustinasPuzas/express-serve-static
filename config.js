const config = {
    PORT: 5001,
    PATH: "my-app" // path to react build folder (relative to server.js) example my-app is in same directory as server.js and build is in my-app
}

module.exports = config;